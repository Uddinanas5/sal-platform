{
  "version": 1,
  "note": "Priority queue. priority = gate severity (P0>P1>P2) × blast radius × evidence strength. The loop pulls the top OPEN, non-blocked item each iteration. New work is appended from: red gates, adversarial disproofs, shrunk property counterexamples, surviving mutants, coverage-ratchet misses, and (when dry) fresh audit rounds. See LOOP.md §5.",
  "updatedAt": null,
  "items": [
    { "id": "L-001", "title": "Wire Stripe TEST-mode E2E: book → pay → webhook → DB row + idempotent replay (Gate B.5)", "gate": "B", "priority": "P0", "state": "open", "notes": "UNBLOCKED Jul 8: Stripe TEST keys found in local .env (sk_test_ + whsec_ + pk_test_), .env gitignored+untracked, DB on dev schema. ⚠ Re-verify STRIPE_WEBHOOK_SECRET (len 70) matches the actual delivering endpoint at runtime — use `npm run stripe:listen` which prints the session signing secret. Never live keys." },
    { "id": "L-002", "title": "Concurrency oracle: fc.scheduledModelRun harness asserting no-overlap + exactly-one-racer-wins on booking (Gate A/J)", "gate": "J", "priority": "P0", "state": "open", "notes": "Strengthen beyond the existing advisory-lock sim to interleaved async commands; fix must be DB constraint / serializable txn, not app check-then-write." },
    { "id": "L-003", "title": "Tenant-override property test: fuzz X-Tenant-ID / body / query; assert server-session tenant wins (Gate A.4)", "gate": "A", "priority": "P0", "state": "done", "notes": "DONE Jul 8 (heartbeat): tests/cross-tenant/tenant-override.a4.test.ts (3 tests) drives the v1 appointments [id] GET+PATCH route with a hostile businessId in body, query, AND X-Tenant-ID header and asserts every Prisma where + the access gate scope to the AUTH tenant, never the injected one. Representative of the withV1Auth/getBusinessContext-derived-tenant pattern; could extend to more routes later. Board-wiring in L-019." },
    { "id": "L-004", "title": "Raw-SQL tenant-predicate grep guard for every $queryRaw/$executeRaw (Gate A.5)", "gate": "A", "priority": "P0", "state": "done", "notes": "DONE Jul 8 (heartbeat): scripts/check-raw-sql-tenancy.mjs + `npm run check:rawsql` + tests/raw-sql-tenancy.test.ts (7 tests). Classifies every raw-SQL site: allows businessId-scoped, pg_advisory_xact_lock (tenant-keyed), SELECT 1 probe, or an explicit `raw-sql-allow:` review note; fails anything else (esp. *Unsafe). Current codebase clean (6 sites, all safe). Board-wiring of A.5 deferred to L-019 (needs owner-approved gate.mjs edit — frozen file)." },
    { "id": "L-005", "title": "Structured-log audit: requestId + shopId on every server log line (Gate C.2)", "gate": "C", "priority": "P0", "state": "open", "notes": "Per-tenant sliceability is launch-critical for multi-tenant." },
    { "id": "L-006", "title": "Set up StrykerJS mutation testing on booking/checkout/auth; gate: new test must lift mutation score, not just coverage", "gate": "meta", "priority": "P1", "state": "open", "notes": "Meta-oracle proving invariants are non-vacuous. Surviving-mutant reports are INTERNAL signal, never handed to the code-writing step." },
    { "id": "L-007", "title": "Metamorphic tests for availability/search API: adding a filter ⇒ Subset; Cardinality never increases (Gate J)", "gate": "J", "priority": "P1", "state": "open", "notes": "No golden output needed; scales to unlabeled inputs." },
    { "id": "L-008", "title": "Adversarial-verifier harness: fresh-context critic re-derives each fix from raw artifacts and tries to disprove it", "gate": "meta", "priority": "P1", "state": "open", "notes": "Every self-reported PASS stays pending until the critic concurs (defeats self-preference bias)." },
    { "id": "L-009", "title": "Secret scan + security headers (TLS/HSTS/CSP) audit as gates (Gate G.3/G.4)", "gate": "G", "priority": "P0", "state": "done", "notes": "DONE Jul 8 (heartbeat): G.4 headers were configured in next.config.mjs but UNGUARDED — extracted the policy to a pure module src/lib/security-headers.mjs + regression-locked it with tests/security-headers.test.ts (5 tests: HSTS prod-only, X-Frame DENY on app routes, /book/* embeddable via frame-ancestors *, nosniff/Referrer/Permissions/self-CSP, no unsafe-eval in prod, Stripe allowlisted). Full build green. Secret-in-source half was already covered by B.1 (secretScan). Remaining G.3 dependency-CVE scan → L-021." },
    { "id": "L-010", "title": "Staging rollback drill: deploy N → N+1 → revert to N; measure (Gate E.3)", "gate": "E", "priority": "P0", "state": "open", "blockedBy": "founder: a staging env mirroring prod", "notes": "Rollback tested, not imagined." },
    { "id": "L-011", "title": "Restore drill wired into CI: actual restore into scratch env, verify row counts + app boots (Gate H.2)", "gate": "H", "priority": "P0", "state": "open", "notes": "A backup that can't be restored isn't a backup. scripts/verify-restore.mjs exists — prove it end-to-end." },
    { "id": "L-012", "title": "Coverage ratchet: ~80% patch coverage on changed lines; fail only if project coverage decreases", "gate": "meta", "priority": "P1", "state": "open", "notes": "Cheapest 'quality only goes up' mechanism." },
    { "id": "L-013", "title": "Per-business staff roles (deferred P1 from TASKS2) — schema migration", "gate": "G", "priority": "P1", "state": "open", "notes": "Carried from round-2 trim mission." },
    { "id": "L-014", "title": "ROADMAP: self-serve API-key + MCP connection UI ('connect your AI agent') — build after P0 battery green", "gate": "feature", "priority": "P2", "state": "open", "notes": "Founder's public product feature. Explicitly LATER — hardening first." },

    { "id": "L-015", "title": "Appointment checkout charges live catalog Service.price but commissions the booked finalPrice snapshot (overcharge + books don't reconcile)", "gate": "B", "priority": "P2", "state": "done", "source": "payments-audit #4 (record-checkout.ts:265)", "notes": "DONE Jul 8 (heartbeat): recordCheckout now, when appointmentId is set, sources each service line's price from the booked AppointmentService.finalPrice snapshot (tenant-scoped query where appointment.businessId) instead of the live catalog price — charge now equals the commission base (which already used finalPrice). Walk-in/POS service lines + all products keep live price. tests/checkout-commission.test.ts +1 (catalog 50 raised vs booked 40 → charges 40). VERIFIED: commission suite 7/7, typecheck+lint clean, golden-path green on dev DB (new query valid), full suite green." },
    { "id": "L-016", "title": "No server-side idempotency on appointment-less (walk-in) checkouts → retry double-records sale, double-spends gift card", "gate": "B", "priority": "P2", "state": "done", "source": "payments-audit #6 + #9 (record-checkout.ts, checkout.ts)", "notes": "DONE Jul 8 (keep-going): added Payment.idempotencyKey + @@unique([businessId, idempotencyKey]) (migration 20260708100000, applied to dev). recordCheckout: fast-path returns the original payment on a repeat key + P2002 catch for a true-concurrent duplicate; key plumbed through all 3 entry points (dashboard action, /api/v1/checkout incl. Idempotency-Key header, MCP tool). tests/checkout-idempotency.test.ts (4 tests). Full suite 582, golden-path green on migrated dev DB. NOTE: audit #10 (create-payment-intent phantom-pending Payment rows) is the ONLINE/Connect path — stays with L-017 (latent until Connect live)." },
    { "id": "L-017", "title": "Online Connect charge has no already-paid guard → appointment can be charged twice across the hourly idempotency window", "gate": "B", "priority": "P2", "state": "open", "source": "payments-audit #8 (create-payment-intent/route.ts:63)", "notes": "LATENT today (Connect onboarding not active in beta + StripePayment UI unmounted). Becomes P1 the instant online payments go live — fix before enabling. Add the already-paid query the other two money paths already use." },
    { "id": "L-018", "title": "Add regression tests for the shipped one-liner payments fixes (#7 custom-line tax toggle; #11 createRefund reverse_transfer)", "gate": "meta", "priority": "P2", "state": "done", "source": "payments-audit fixes #7/#11 shipped without dedicated tests", "notes": "DONE Jul 8 (heartbeat): tests/stripe-refund.test.ts (2 — mocks the Stripe SDK, asserts createRefund passes reverse_transfer:true + surfaces failures) + tests/checkout-custom-tax.test.ts (2 — tax-off shop leaves a $100 Quick Sale at $100; default config taxes it). #3 (api/bookings tax-exclusive finalPrice) route test split to L-023 (disproportionate route mocking; the fix is a one-liner already aligned to every other creation path)." },
    { "id": "L-019", "title": "OWNER-APPROVED: wire Gates A.4 + A.5 into loop/gate.mjs (frozen file) so the board flips from the new tenant guards", "gate": "A", "priority": "P1", "state": "open", "owner": true, "notes": "Add to RUNNERS: `\"A.5\": { needsDb: false, run: () => exec(\"npm\", [\"run\", \"check:rawsql\"]) }` and `\"A.4\": { needsDb: false, run: () => exec(\"npx\", [\"vitest\", \"run\", \"tests/cross-tenant/tenant-override.a4.test.ts\"]) }`. gate.mjs is CODEOWNERS-protected; the heartbeat won't self-edit its own grader. Merge via PR review." },
    { "id": "L-020", "title": "Dashboard: generate + send an idempotencyKey from payment-dialog.tsx per checkout attempt (make the server L-016 guard effective for the primary UI)", "gate": "B", "priority": "P2", "state": "done", "source": "L-016 follow-up", "notes": "DONE Jul 8 (heartbeat): payment-dialog.tsx now holds a crypto.randomUUID() idempotency key per checkout session (useRef), minted on first submit, KEPT across retries so a double-click/network-blip retry dedupes to the original payment server-side, reset when the dialog reopens for a new sale. Passed to processPayment. VERIFIED: typecheck+lint clean, full production build green. The no-double-charge INVARIANT is already locked server-side by tests/checkout-idempotency.test.ts; this is thin client wiring into that guarded path (no component-test infra in the repo to unit-test the wiring itself). Completes the L-016 story end-to-end." },
    { "id": "L-021", "title": "Dependency vulnerability scan as a gate (Gate G.3): fail on unaddressed critical/high CVEs", "gate": "G", "priority": "P1", "state": "done", "source": "L-009 split", "notes": "DONE Jul 8 (heartbeat): scripts/check-audit.mjs + `npm run check:audit` + audit-allowlist.json + tests/check-audit.test.ts (5). Fails on any high/critical prod-dep vuln NOT triaged in the allowlist; prints all high/critical transparently (accepted vs UNTRIAGED); warns on overdue reviewBy. Current scan: 0 critical, 4 high (all triaged & documented). Actual remediation of the 4 highs = L-022 (human-gated)." },
    { "id": "L-022", "title": "ESCALATION: remediate the 4 triaged high CVEs (next / fast-uri / path-to-regexp / picomatch)", "gate": "G", "priority": "P1", "state": "open", "owner": true, "source": "L-021 findings", "notes": "next [high, DoS] fix = Next 16 MAJOR upgrade (14.2.x→16) — human decision + full regression pass. fast-uri/path-to-regexp/picomatch [high, path-traversal/ReDoS, transitive] = fixable via package overrides but needs an npm + pnpm dual-lockfile sync (Vercel = pnpm --frozen-lockfile) + build verify. All DoS/ReDoS/path-traversal class, not RCE/data-leak. Details + reviewBy 2026-08-01 in audit-allowlist.json." },
    { "id": "L-023", "title": "Route test for POST /api/bookings tax-exclusive finalPrice (payments-audit #3)", "gate": "B", "priority": "P3", "state": "open", "source": "L-018 split", "notes": "The #3 fix (store tax-exclusive finalPrice, matching every other creation path) shipped without a dedicated test — a route test needs heavy mocking (getBusinessContext + location/service/availability/appointment.createMany). Low risk (one-liner, aligned to all other paths); worth a focused route test when convenient." }
  ]
}
